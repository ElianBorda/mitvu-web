export type EventoBody = {
    titulo: string,
    descripcion: string,
    fecha: string,
    comision_id: string,
}

export type Evento = {
    id: string,
    titulo: string,
    descripcion: string,
    fecha: string,
    comision_id: string
}